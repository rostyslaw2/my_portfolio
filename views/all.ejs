<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Всі користувачі</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .user {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            background-color: #fff;
            padding: 10px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            overflow: hidden;
            margin-right: 20px;
        }

        .avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .user-details {
            flex: 1;
        }

        .user-name {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .user-age {
            font-size: 16px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Всі користувачі</h1>
        <% if(AllCart.length > 0) { %>
            <% AllCart.forEach(user => { %>
                <div class="user">
                    <div class="avatar">
                        <img src="<%= user.avatar %>" alt="Avatar">
                    </div>
                    <div class="user-details">
                        <div class="user-name"><%= user.name %></div>
                        <div class="user-age">Вік: <%= user.age %></div>
                    </div>
                </div>
                <form action="/delete/<%= user._id %>" method="post">
                    <button type="submit">Delete</button>
                </form>
            <% }); %>
        <% } else { %>
            <p>Немає жодного користувача</p>
        <% } %>
    </div>
</body>
</html>
